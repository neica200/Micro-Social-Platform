<br />
<h2 class="text-center">Afisare postari</h2>
<br />

@if (ViewBag.Message != null)
{
    <h2 class="p-3 rounded-3 text-center mb-5 alert @ViewBag.Alert">@ViewBag.Message</h2>
}
<br />

@* Buton New – doar pentru user logat *@
@if (ViewBag.IsLogged == true)
{
    <div class="text-center mb-4">
        <a class="btn btn-success"
           asp-controller="Posts"
           asp-action="New">
            Adauga postare
        </a>
    </div>
}


@* Lista postarilor *@
@foreach (var post in ViewBag.Posts)
{
    var commentsCount = post.Comments.Count;
    var reactionsCount = post.Reactions.Count;

    <a asp-controller="Posts"
       asp-action="Show"
       asp-route-id="@post.Id"
       style="text-decoration:none; color:inherit;">

    <div class="card mb-4">
        <div class="card-body">

            <h5 class="card-title">@post.User.UserName</h5>
            <h6 class="card-subtitle mb-2 text-muted">
                @post.CreatedAt.ToLocalTime().ToString("dd MMM yyyy, HH:mm")
            </h6>

            <hr />

           

            @if (post.ImageUrl != null && post.ImageUrl != "")
            {
                <img src="@post.ImageUrl" class="img-fluid rounded mb-2" />
            }

            @if (post.VideoUrl != null && post.VideoUrl != "")
            {
                <video class="w-100 rounded mb-2" controls>
                    <source src="@post.VideoUrl" />
                </video>
            }
                @if (post.Content != null && post.Content != "")
                {
                    <p class="card-text" style="white-space: pre-wrap;">
                        @post.Content
                    </p>
                }

            <hr />

            <p class="text-muted">
                ❤️ @reactionsCount reactii
                💬 @commentsCount comentarii |
                
            </p>
                
      
    </div>
    </a>
}


@* 
<div class="container my-4" style="max-width: 760px;">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h3 class="m-0">Posts</h3>

        @if ((bool)ViewBag.IsLogged)
        {
            <a class="btn btn-primary" asp-action="New">New post</a>
        }
    </div>

}

    @foreach (var post in ViewBag.Posts)
    {
        var commentsCount = post.Comments.Count();   // sau Count(c => !c.IsDeleted)
        var reactionsCount = post.Reactions.Count(); // sau Count(r => !r.IsDeleted)

        var displayName = post.User?.UserName ?? "Unknown";
        var dateText = post.CreatedAt.ToLocalTime().ToString("dd MMM yyyy, HH:mm");

        <div class="card shadow-sm mb-3">
            <div class="card-body">
                <div class="d-flex justify-content-between align-items-start mb-2">
                    <div>
                        <div class="fw-semibold">@displayName</div>
                        <div class="text-muted" style="font-size: 0.9rem;">@dateText</div>
                    </div>

                    @{
                        bool logged = (bool)ViewBag.IsLogged;
                        bool isAdmin = (bool)ViewBag.IsAdmin;
                        string? me = ViewBag.UserCurent as string;

                        bool isOwner = logged && post.UserId == me;
                        bool canEdit = isOwner;                 // admin NU editează
                        bool canDelete = isOwner || isAdmin;    // admin poate șterge
                    }

                    <div class="d-flex gap-2">
                        @if (canEdit)
                        {
                            <a class="btn btn-sm btn-outline-secondary"
                               asp-action="Edit" asp-route-id="@post.Id">Edit</a>
                        }

                        @if (canDelete)
                        {
                            <form asp-action="Delete" asp-route-id="@post.Id" method="post">
                                @Html.AntiForgeryToken()
                                <button class="btn btn-sm btn-outline-danger" type="submit">Delete</button>
                            </form>
                        }
                    </div>
                </div>

                @if (!string.IsNullOrWhiteSpace(post.Content))
                {
                    <p class="mb-2" style="white-space: pre-wrap;">@post.Content</p>
                }

                @if (!string.IsNullOrWhiteSpace(post.ImageUrl))
                {
                    <img src="@post.ImageUrl" class="img-fluid rounded mb-2" alt="post image" />
                }

                @if (!string.IsNullOrWhiteSpace(post.VideoUrl))
                {
                    <video src="@post.VideoUrl" class="w-100 rounded mb-2" controls></video>
                }

                <div class="d-flex justify-content-between align-items-center pt-2 border-top">
                    <div class="text-muted" style="font-size: 0.95rem;">
                        💬 @commentsCount &nbsp;&nbsp; ❤️ @reactionsCount
                    </div>

                    @if ((bool)ViewBag.IsLogged)
                    {
                        <a class="btn btn-sm btn-outline-primary"
                           asp-action="Show" asp-route-id="@post.Id">Open</a>
                    }
                    else
                    {
                        <span class="text-muted" style="font-size: 0.9rem;">
                            Log in pentru like/comment
                        </span>
                    }
                </div>
            </div>
        </div>
    }


</div> *@
